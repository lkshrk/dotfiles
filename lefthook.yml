# yaml-language-server: $schema=https://json.schemastore.org/lefthook.json
# yaml-language-server: $schema=https://json.schemastore.org/lefthook.json
# yaml-language-server: $schema=https://json.schemastore.org/lefthook.json
# Lefthook config for the dotfiles repo.
# Activate with: lefthook install (also called by setup.sh).

pre-commit:
  commands:
    gitleaks:
      run: gitleaks protect --staged --redact

pre-push:
  commands: {}
